# Telegram bot with webhook support
python-telegram-bot[webhooks]==21.0

# For HTTP requests to APIs (Sarf-Today and CurrencyLayer)
requests==2.31.0

# Optional: useful for datetime parsing if needed
python-dateutil==2.9.2
